spring.application.name=social

# ===================== دیتابیس =====================
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/social_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Arman@1365}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# ===================== دامنه و آدرس‌ها (این بخش را روی سرور تغییر بده) =====================
# آدرس پایه که برای ساخت لینک فایل‌های آپلودی استفاده می‌شود
app.base-url=${APP_BASE_URL:http://localhost:8080}

# پوشه ذخیره‌سازی فایل‌های آپلودی
app.upload-dir=${APP_UPLOAD_DIR:uploads/}

# آدرس فرانت‌اند برای CORS (چند آدرس را با کاما جدا کن، بدون اسلش انتهایی)
app.cors.allowed-origins=${APP_CORS_ORIGINS:http://localhost:5173}

# ===================== JWT =====================
# حتماً روی سرور یک مقدار طولانی و تصادفی (حداقل ۳۲ کاراکتر) بذار
app.jwt.secret=${APP_JWT_SECRET:o89CiGTKvmENQnUxVaixE5a+zxburIZjQLxVtwte+Fk=}
app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}
app.upload-base-url=${APP_BASE_URL:http://localhost:8080}

# ===== Kavenegar (برای شماره‌های +98) =====
kavenegar.api-key=
# اختیاری؛ اگر شماره اختصاصی/سرشماره دارید اینجا بگذارید، وگرنه خالی بگذارید
kavenegar.sender=

# ===== Twilio (برای بقیه دنیا) =====
twilio.account-sid=
twilio.auth-token=
# شماره‌ای که از پنل Twilio خریداری/تخصیص دادید (فرمت E.164، مثلا +1XXXXXXXXXX)
twilio.from-number=

# ===== Gmail SMTP (برای ایمیل) =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=amir.hendianpour@gmail.com
# این رمز عادی جیمیل نیست! باید "App Password" بسازید (توضیح پایین پیام)
spring.mail.password=vmjoclbzlwloscwy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
